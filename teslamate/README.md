# Teslamate auth

Teslamate requires an access token and a refresh token that can be generated via the `tesla_auth` binary. this should be a one time setup. they are not stored anywhere but should be automatically refreshed by Tesla once Teslamate is authenticated with the initial tokens.
